# Ch2 神经网络与深度学习基础

> 一句话简介：搭建起"神经元 → 损失函数 → 反向传播 → 优化器"的最小心智模型，为理解 Transformer 训练做准备。

## 本章目标

读完本章你应当能：
- 画出一个多层感知机（MLP）并口述其前向计算
- 解释反向传播的核心思想（不必手推）
- 说出常见激活函数（ReLU / GELU / Softmax）的用途与差异
- 区分 MSE 与交叉熵损失的使用场景
- 比较 SGD / Adam / AdamW 优化器的差异
- 说出 Dropout、Weight Decay、BatchNorm / LayerNorm 的目的

## 本章导览

```mermaid
graph LR
    A[输入 x] --> B[线性变换 Wx+b]
    B --> C[激活函数]
    C --> D[下一层]
    D --> E[输出预测]
    E --> F[计算损失]
    F --> G[反向传播]
    G --> H[优化器更新权重]
    H --> B
```

## 2.1 神经元与多层感知机

**人工神经元**是神经网络的基本单元，它做的事情非常朴素：把若干输入按权重加和、加上偏置、再过一个非线性函数。一个神经元的输出可以写成 $y = \sigma(w_1 x_1 + w_2 x_2 + \dots + w_n x_n + b)$，其中 $\sigma$ 是激活函数，$w$ 是权重，$b$ 是偏置。可以把它类比为一个加权投票器——每个输入按重要性打分，最后汇总成单一输出。

把多个神经元排成一层、把多层堆叠起来，就得到**多层感知机（Multi-Layer Perceptron, MLP）**。典型的 MLP 包含输入层、若干隐藏层和输出层；每一层的输出作为下一层的输入，层层"加工"。我们这本书记号上把输入维数为 1、两个 32 维隐藏层、输出维数为 1 的网络记为 1 → 32 → 32 → 1。本章配套代码 `train_mlp.py` 用的就是这个结构。

那为什么一定要在层与层之间插入非线性激活函数？因为如果只有线性变换，多层堆叠等价于一个更大的线性变换——模型永远只能表达直线或平面，拟合能力非常有限。引入非线性后，整个网络才变成一个"通用函数逼近器"，理论上可以拟合任意复杂的连续函数。**这也是为什么一个没有激活函数、只有线性层的"深度网络"，本质上和单层线性回归没有区别。**

## 2.2 激活函数

激活函数负责给神经网络"加非线性"。常用的有三个，各有分工。

**ReLU（Rectified Linear Unit）** 是最常见的激活函数，定义为 $f(x) = \max(0, x)$——正数原样输出、负数归零。它由 Nair 和 Hinton 在 2010 年 ICML 论文中正式推广（参见本章参考）。ReLU 最大的优点是简洁、计算快，而且在正区间梯度恒为 1，有效缓解了深层网络中常见的梯度消失问题，是 CNN 和 MLP 的默认选择。它的缺点是"死神经元"——一旦某个神经元输出落到负区间且梯度为 0，它就再也不会更新。

**GELU（Gaussian Error Linear Unit）** 是 Transformer 时代的默认激活，定义为 $f(x) = x \cdot \Phi(x)$，其中 $\Phi$ 是标准正态分布的累积分布函数。直观上，GELU 比 ReLU 更平滑——它在 0 附近不是硬截断而是软过渡，给所有输入都留一点点非零梯度，训练更稳定。GPT、BERT 等主流模型都用 GELU。

**Softmax** 跟前面两个不一样，它专门用在多分类任务的输出层。它把一组实数（logits）转换成概率分布：每个值先指数化，再除以所有指数之和，保证输出和为 1、每个值在 (0, 1) 之间。这样模型最后一层的输出就能直接解读为"属于每一类的概率"。后面 Ch4 会看到，LLM 在每个位置上预测下一个 token，本质上就是一个词表大小（几万到十几万）的多分类任务，输出层用的就是 Softmax。

## 2.3 反向传播的直觉

**反向传播（Backpropagation）** 是训练神经网络的核心算法。我们不需要手推公式，但必须抓住它的直觉。

训练的目标：调整每一层的权重 $W$ 和偏置 $b$，让损失函数的值尽可能小。损失函数衡量"模型预测"与"真实答案"之间的差距。函数告诉我们"在当前权重下，预测错得有多离谱"，但没直接告诉我们"权重该怎么改"。

反向传播回答的就是"权重该怎么改"这个问题。它的核心思想是**链式法则**——损失对某一层权重的梯度，可以从"损失对输出的梯度"沿网络反向逐层相乘得到。换句话说，**梯度像水流一样从损失端往输入端流回每一层**。每一层根据流到本层的梯度，知道"自己该往哪个方向调、幅度多大"。

实际工程里没人手算这些梯度。PyTorch 用**自动微分（autograd）**机制——你只需要写前向计算（`loss = loss_fn(model(x), y)`），调用一次 `loss.backward()`，PyTorch 就沿着你刚才建立的计算图反向遍历，自动算出所有参数的梯度；优化器再根据这些梯度更新参数。整个过程对用户几乎是透明的。

## 2.4 损失函数

损失函数（Loss Function）量化模型预测与真实答案的差距，是训练的"指挥棒"。任务不同，指挥棒也得换。

**均方误差（Mean Squared Error, MSE）** 用于回归任务，定义为预测值与真实值之差的平方的平均：$L = \frac{1}{N} \sum (y_i - \hat{y}_i)^2$。它对大误差的惩罚更重（平方放大了差距），训练出来的模型倾向于平滑预测。它不要求输出是概率，适合"预测房价""预测温度"这种连续值输出。本章配套代码 `train_mlp.py` 就用 MSE 拟合正弦曲线。

**交叉熵损失（Cross-Entropy Loss）** 用于分类任务。它衡量两个概率分布之间的"距离"——预测分布越接近真实分布（one-hot 标签），损失越小。配合 Softmax 输出层时，公式有个非常简洁的等价形式：$L = -\log(\hat{y}_{\text{correct class}})$，即"正确答案对应的预测概率取负对数"。如果模型对正确类别给出 0.99 的概率，损失约 0.01；如果只给 0.1，损失高达 2.3——逼迫模型在正确选项上"加把劲"。

简单记法：**连续值用 MSE，离散分类用交叉熵**。LLM 的语言模型头也是分类——从词表里挑下一个 token，所以训练时用的就是交叉熵（具体细节见 Ch4）。

## 2.5 优化器演进

**优化器（Optimizer）** 负责根据梯度更新参数。三代主流优化器值得了解。

**SGD（随机梯度下降）** 是最朴素的形式：$W \leftarrow W - \eta \cdot \nabla L$。学习率 $\eta$ 是唯一超参，控制"每一步迈多大"。SGD 思路简单、稳定性好，但所有参数共用同一个学习率，对稀疏梯度或不同尺度的参数不太友好。

**Adam（Adaptive Moment Estimation）** 是 2014 年 Kingma 和 Ba 提出的优化器，2015 年发表于 ICLR（参见本章参考）。它为**每个参数维护自适应的学习率**——结合了一阶矩（梯度的指数移动平均，类似动量）和二阶矩（梯度平方的指数移动平均，类似 RMSProp）。Adam 收敛快、调参省事，是深度学习事实上的默认优化器。

**AdamW（Adam with decoupled Weight Decay）** 由 Loshchilov 和 Hutter 在 2019 年提出。它发现 Adam 里的 L2 正则项其实并没真正起正则作用，于是把权重衰减从梯度更新里"解耦"出来，直接作用在参数上：$W \leftarrow W - \eta \cdot (\nabla L + \lambda W)$。这一改动让训练更稳定、泛化更好，是训练现代 Transformer 的标配，本书后续章节默认使用 AdamW。

## 2.6 正则化

正则化（Regularization）的目的只有一个：防止模型在训练集上学得太好、在测试集上却表现糟糕（**过拟合**）。两个最常用的工具是 Dropout 和 Weight Decay。

**Dropout（随机失活）** 由 Srivastava 等人在 JMLR 2014 正式系统化（参见本章参考）。做法很简单：训练时每一轮前向传播，对每个神经元以概率 $p$（常见 0.5）随机"关闭"——输出置零；测试时则使用全部神经元并按 $p$ 缩放权重。直觉是：每次前向传播都在用一个"瘦了"的子网络，迫使模型不能依赖任何单个神经元。Dropout 在全连接层和 RNN 里特别常用，现代 Transformer 因为有 LayerNorm 和大量数据，Dropout 用得反而少。

**Weight Decay（权重衰减）** 是另一类正则：给损失函数加一个 $\lambda \|W\|^2$ 惩罚项，等价于让权重在每步更新里都略微往 0 收缩。AdamW 里的"W"就是这套机制的现代实现。

简单记法：**Dropout 管结构（神经元要不要参与），Weight Decay 管参数（参数不要太大）**。

## 2.7 归一化

归一化（Normalization）是深度学习里另一类不可或缺的工具，目的都是让激活值的分布更稳定，从而加速收敛、提高泛化。

**Batch Normalization（批归一化, BN）** 由 Ioffe 和 Szegedy 在 2015 年提出（参见本章参考）。做法是：对每个特征通道，在一个 mini-batch 内部计算均值和方差，把激活值标准化到均值为 0、方差为 1，再通过两个可学习参数 $\gamma$ 和 $\beta$ 做仿射变换。BN 的好处是允许更大的学习率、对初始化不敏感、还自带正则效果。**BN 沿"batch 维度"归一化**，所以对 batch size 敏感，在 RNN 和小 batch 场景下不太友好。它是 CNN 时代的标配（ResNet 几乎所有卷积层后都接 BN）。

**Layer Normalization（层归一化, LN）** 由 Ba、Kiros 和 Hinton 在 2016 年提出。它和 BN 的关键区别是**沿"特征维度"归一化**——对单个样本内的所有特征计算均值和方差，独立于 batch。LN 不受 batch size 影响，天然适合序列模型，所以成为 Transformer 的标配（每一层 Attention 和 FFN 之后都接一个 LN）。这也是为什么 GPT、BERT、LLaMA 等所有主流 LLM 都用 LN 而不是 BN。

| 维度 | BatchNorm | LayerNorm |
|---|---|---|
| 归一化范围 | 跨样本（同一通道） | 跨特征（同一样本） |
| 依赖 batch size | 是 | 否 |
| 主要应用 | CNN | Transformer / RNN |

归一化层通常放在**线性变换之后、激活函数之前**（"post-norm"）或**激活之后**（"pre-norm"，Transformer 默认），位置对训练稳定性影响很大。现代 LLM 几乎都用 pre-norm + LayerNorm，细节见 Ch3。

## 2.8 一个最小的 PyTorch 训练示例

光讲概念不够直观，我们用一个最小可运行的例子把上面所有零件串起来。完整代码见 `code/part-1/train_mlp.py`。

**模型定义**：MLP 类继承自 `nn.Module`，由 `nn.Sequential` 串起 3 个线性层和 2 个 ReLU——结构就是 2.1 节说的 1 → 32 → 32 → 1。

**数据准备**：用 `torch.linspace` 在 $[-2\pi, 2\pi]$ 上取 200 个点，目标是 `y = sin(x)`。这是一个非线性函数，没有激活函数的纯线性模型绝对拟合不了，正好用来验证 MLP 的非线性能力。

**训练三件套**：损失函数用 `nn.MSELoss`（回归问题）；优化器用 `optim.Adam`，学习率 1e-2，对这个小网络正合适；训练循环 1000 步，每步做 4 件事——前向算 `loss`、清梯度、`loss.backward()` 反向传播、`optimizer.step()` 更新参数。这是 PyTorch 训练的"标准四步曲"，几乎所有训练脚本都长这样。

**收敛判断**：循环结束后再算一次 `final_loss`，并用 `assert final_loss < 0.01` 断言它必须低于 0.01。这是一种"自检"习惯——如果代码改动后这个断言失败，说明改动破坏了训练的某处。

直接运行 `python code/part-1/train_mlp.py` 应当看到 5 行 `step` 日志（步 0 到 800）和 1 行 `final loss`，断言 `final loss < 0.01` 通过、退出码 0。这个例子虽然简单，但已经包含了后续训练 Transformer 所需的全部核心元素：**模型 + 数据 + 损失 + 优化器 + 反向传播**。理解了它，就理解了 Ch4 预训练循环的 80%。

## 本章小结

- 神经元 = 加权求和 + 偏置 + 非线性激活；MLP 是多层神经元堆叠，必须有非线性才能拟合复杂函数。
- 常见激活函数：ReLU（简洁、CNN/MLP 默认）、GELU（平滑、Transformer 默认）、Softmax（多分类输出转概率）。
- 反向传播的核心是链式法则——梯度从损失端反向流回每一层；PyTorch 的 autograd 让这一切自动完成。
- 损失函数：连续值回归用 MSE，离散分类用交叉熵；LLM 训练本质上是 token 级多分类。
- 优化器从 SGD → Adam（自适应学习率）→ AdamW（解耦权重衰减）；Transformer 训练默认用 AdamW。
- 正则化：Dropout 随机失活防结构过拟合，Weight Decay L2 惩罚防参数过大。
- 归一化：BatchNorm 沿 batch 维度（CNN 默认）、LayerNorm 沿特征维度（Transformer 默认）。

## 本章参考

### 必读论文

- [Adam: A Method for Stochastic Optimization (Kingma & Ba, 2015)](https://arxiv.org/abs/1412.6980) — 引入 Adam 优化器，每个参数自适应学习率，是深度学习默认优化器。
- [Dropout: A Simple Way to Prevent Neural Networks from Overfitting (Srivastava et al., 2014)](https://jmlr.org/papers/v15/srivastava14a.html) — JMLR 论文，系统化 Dropout 训练与推理机制，成为后续深度模型标配。
- [Batch Normalization (Ioffe & Szegedy, 2015)](https://arxiv.org/abs/1502.03167) — 提出 BatchNorm，让训练深层网络变得稳定可复现。

### 推荐博客 / 教程

- [PyTorch 官方 60 分钟入门](https://pytorch.org/tutorials/beginner/deep_learning_60min_blitz.html) — PyTorch 团队出品的快速上手教程，张量、自动微分、nn.Module 与训练循环一站式讲清。
- [3Blue1Brown 神经网络与梯度下降](https://www.3blue1brown.com/topics/neural-networks) — 视频化的神经网络直觉讲解，从感知机到反向传播，看完对几何直观很有帮助。

### 视频 / 课程

- [吴恩达 Machine Learning 课程 (Coursera)](https://www.coursera.org/learn/machine-learning) — 经典入门课，包含神经网络的直观讲解与手推梯度。
- [李宏毅 机器学习 (台大公开课)](https://speech.ee.ntu.edu.tw/~hylee/ml/2021-spring.php) — 中文讲解，PyTorch 实例丰富，与本书配套度高。
